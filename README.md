**First you need to create the .env file to which the Mysql will be connected. 
.env File content:
DB_USER=root
DB_NAME=waedi_ch
DB_PASS=your_password  
PORT=3001
HOST=127.0.0.1

then 

npm install

and 

npm start